{
  "name": "rollacoasta-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RollaInstruments — breedable market-instrument NFTs whose genome is a price-engine parameter set.",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "compile": "node scripts/compile.mjs",
    "test": "forge test && node test/genome.test.mjs",
    "test:forge": "forge test",
    "test:genome": "node test/genome.test.mjs",
    "deploy": "node scripts/deploy.mjs"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "5.1.0",
    "solc": "0.8.28"
  }
}
